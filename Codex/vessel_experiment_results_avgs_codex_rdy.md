CODEX IMPLEMENTATION PROMPT — COPY INTO CODEX

You are to implement a new Python module that processes timeseries CSV datasets for multiple reactors and extracts experiment-window averages. Use the following specifications to generate clean, readable, and refactor-friendly code.

OBJECTIVE

Load all target datasets, detect experiment windows using energy_use_R#_normalized values, extract mean values of specified columns across each experiment window, assemble results in flattened dataframes split by reactor, and save to specified output directory.

⸻

DATA + PATHS
	•	Input datasets directory:
naive_stacked_data/with_snh4_norm/
	•	Output directory to create if missing:
naive_stacked_data/with_snh4_norm/vessel_experiment_results/

⸻

TARGETS

Only process datasets whose parsed reactor_no ∈ [3, 4, 5]

The following fields should be averaged per experiment window:

average_column_targets = [
    "SNH4_inf_nom",
    "SNH4_nom_norm",
    "SNH4_nomdiff_norm"
]

The normalized energy column name must be derived dynamically from filename parse:

energy_use_R{reactor_no}_normalized


⸻

EXPERIMENT WINDOW RULES

Simulation timesteps are clean + contiguous — do not code missing-value handling.

Row index staggering is used to detect sequential experiments:

Parameter	Value	Meaning
timestep_len	4	timesteps per hour
timestep_day_len	24 * 4	timesteps per day
exp_spread_days	3	interval between experiments

So:

index_stagger = exp_spread_days * timestep_day_len

Window detection method:
	1.	First experiment start index = row where energy_col == energy_reduc (from filename)
	2.	First experiment end index = start_index + (duration * timestep_len - 1)
	3.	Subsequent windows = generated by stagger offsets until beyond dataframe length.

⸻

OUTPUT STRUCTURE (NO DICTIONARIES — FLATTENED)

Create one dataframe per reactor:

| duration | energy_reduc | avg_SNH4_inf_nom | avg_SNH4_nom_norm | avg_SNH4_nomdiff_norm |

Append one row per experiment window.

Then save e.g.:

reactor3_experiment_summary.csv
reactor4_experiment_summary.csv
reactor5_experiment_summary.csv


⸻

IMPLEMENTATION REQUIREMENTS
	•	Use pandas
	•	Modularize — create functions for:
	1.	filename parsing
	2.	experiment window detection
	3.	averaging logic
	4.	dataframe assembly + export
	•	Keep code maintainable — clear variable names, no deep nesting
	•	No premature optimization — clarity > speed
	•	Avoid dictionary-of-lists structures — only flattened dataframes

⸻

🔨 CODE SCAFFOLDING FOR CODEX TO COMPLETE

import os
import pandas as pd
import re

INPUT_DIR  = "naive_stacked_data/with_snh4_norm/"
OUTPUT_DIR = "naive_stacked_data/with_snh4_norm/vessel_experiment_results/"
TARGET_REACTORS = [3,4,5]
TIMESTEP_LEN = 4
TIMESTEP_DAY = 24 * TIMESTEP_LEN
EXP_DAYS = 3
INDEX_STAGGER = EXP_DAYS * TIMESTEP_DAY

average_column_targets = [
    "SNH4_inf_nom",
    "SNH4_nom_norm",
    "SNH4_nomdiff_norm"
]

# ---------------------------------------------------------
# 1. Parse reactor, duration, day_start, and energy_reduc from filename
def parse_filename(filename):
    # implement regex extraction
    ...

# ---------------------------------------------------------
#  2. Identify experiment window start + end indices
def detect_experiment_windows(df, energy_col, energy_reduc, duration):
    # compute first experiment window
    # compute subsequent windows using INDEX_STAGGER
    ...

# ---------------------------------------------------------
#  3. Compute averages per window
def compute_window_means(df, windows, columns):
    # return list of {duration, energy_reduc, means...}
    ...

# ---------------------------------------------------------
#  4. Build final summary dataframe per reactor
def build_reactor_dataframe(records):
    # return flattened dataframe
    ...

# ---------------------------------------------------------
#  MAIN PIPELINE
def run_pipeline():
    # 1. scan INPUT_DIR
    # 2. parse + select reactor X files
    # 3. compute experiment windows + means
    # 4. assemble dataframe
    # 5. export CSV to OUTPUT_DIR
    ...

if __name__ == "__main__":
    run_pipeline()